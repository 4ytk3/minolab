from core.entities.image_data import ImageData

class CustomAlgorithmUseCase:
    def execute_algorithm(self, image_data: ImageData) -> ImageData:
        # 複数の画像処理を組み合わせたカスタムアルゴリズムを実行
        return image_data
